#!/bin/bash
# "Answer is out" - the agent says so after replying in text only. The badge drops
# "думаю" on this file; with voice the speak lock does the same job by itself.
touch "$HOME/.claude/jarvis/answer.done"
