# Jarvis voice session

Lightweight working directory for the Jarvis voice assistant. Deliberately empty
of project context: everything heavy (the global CLAUDE.md, hooks, MCP servers)
would be re-sent on every voice question and cost seconds of latency.
